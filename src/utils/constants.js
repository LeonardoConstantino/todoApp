// src/utils/constants.js
/**
 * Prefixo da aplicação de tarefas (to-do).
 * Esse prefixo é usado em todo o aplicativo para identificar recursos relacionados a ele.
 * @constant {string} APP_PREFIX - Prefixo da aplicação de tarefas (to-do).
 */
export const APP_PREFIX = 'todoApp';
